## Active frame/patch repair — 2026-09-23

The user authorized all required P4 design repairs. `grounded-observations-6` implements pre-extraction source-grounded experiment frames/context recovery, source metadata preservation, bounded observation patches with unchanged-field enforcement, and finding-level disposition separate from bundle coverage. P4 context bounds are eight parts/16,000 serialized characters; frozen P3/source artifacts remain unchanged. Final KG vocabulary and models remain unchanged.

Bounded live validation is running as `trial-4ade5b51625020db` on joint_gene_loss, organoid_endpoints and expression_table, using GLM extraction/Kimi verification with the recorded 180-second deadline. Inspect `data/work/p4-accuracy/frame-patch.log`, the trial status and `frame-patch-state.json`; do not launch duplicates. 94 focused tests and 72 corpus/parser tests passed. The completed intermediate trial `trial-b98e14e6a0495cef` demonstrated a one-field table repair but exposed oversized joint-loss verification and incorrect frame selection. The current run uses two-finding review batches, matching-frame measured-variable enforcement and one bounded frame-amendment pass for missed/context-defective findings. The initial frame diagnostic `trial-9291684d0a824962` was stopped after duplicate field names were detected; fixed named frame slots supersede the list contract, with completed caches and unknown dispatch reservations preserved. No full-corpus restart or accuracy claim yet. Earlier pause text below is historical and superseded by this explicit user authorization.

Continue this existing project as the lead coding/review collaborator.

LATEST STEERING: the user asked to wait and reread all three ChatGPT synthesis/schema documents. This is complete (5,900 lines; receipt `reports/continuation/p4/context-review.json`), and live inference is paused. Current code is grounded-observations-5 / spatial-nichelinker-kg-2 with 80 focused tests passing. Read the current CONTINUE_HERE and handoff before the historical version-4 checkpoint below. Preserve `trial-e141d8b129661e28` partial state and unknown reservations; no job remains scheduled. The reread corrects an overreach: independent gold annotation is needed for numerical accuracy claims, not a mandatory separate project blocking this no-curator manuscript KG. Later explicit user additions for bounded critique/repair and reusable annotation candidates remain authorized.

Start with docs/CONTINUE_HERE.md and docs/RESUME.md, then AGENTS.md, the assigned P4 scope in plan.md, docs/P4_CRITIQUE_AUTOMATION.md and docs/handoffs/P4.md. Follow newer explicit user instructions over historical checkpoints.

The latest completed instruction was “i want this automated..do now.” The ordinary `grounded-observations-4` engine now performs PubTator-assisted mention recognition, GLM extraction, blind Kimi source reading, field/mention/fact criticism, one semantic repair and fresh verification. Missing source context routes to the existing bounded local assembly agent. Extractor/critic remain tool-free. Final KG vocabulary and evidence semantics come from configs/kg_schema.yaml; scientific extraction instructions come from configs/extraction_schema.yaml. Both requested final synthesis/schema documents were read completely in the prior alignment; reference/ remains read-only.

Live diagnostic `trial-2c83876001d2386b` completed three frozen source bundles from three papers. Both nonempty cases repaired and reverified automatically, but all eleven final observations remain rejected; the atlas source produced no eligible relation and passed its audit. Read reports/continuation/p4/critique-repair-review.md and its JSON receipt. Seventy-seven focused synthetic tests passed; identical replay used zero new calls. These facts do not establish improved NER/RE accuracy or gold annotations. No inference job remains scheduled. Check status/lock before any new process and do not rerun completed diagnostics without a concrete new question.

Use the existing .venv and preserve .env. Ordinary roles resolve to LiteLLM `prescient.glm-5.3-flash` extraction and `prescient.kimi-k3` verification through existing env/YAML precedence and project TLS settings. Both aliases are authorized for bounded implementation validation; no OpenRouter fallback is permitted. No source acquisition, parsing, screening, embedding or snapshot restoration is needed. PubTator uses existing source-aligned annotations; do not claim that a local BioREx relation model ran.

Source selection remains p3selection-89efefdd214ee16e: 156 selected papers, 115 usable main texts and 1,133 ready bundles; 41 main texts remain on the download ledger. Those counts do not establish comprehensive manuscript coverage. Preserve all four lineages and unresolved scientific context.

Remaining P4 work is evidence-classification and gene/protein annotation consistency, quantity semantics and source-preserving overflow handling, followed by independently reviewed complete annotations and precision/recall evaluation. The repair misclassified primary evidence as CURATED_LITERATURE; some quantity references and entity types also remain wrong. Model critics can err. Keep orchestration deterministic and preserve unresolved findings instead of weakening gates. Candidate annotation export is authorized, but current outputs have zero gold/training-eligible labels; see docs/P4_ANNOTATION_DESIGN.md. Actual encoder training and P5 graph work are outside this continuation.

Full-corpus extraction remains stopped in data/work/p4-corpus/status.json (`restart_authorized: false`). Preserve the newer diagnostic outputs/caches and cumulative data/work/p4-validation/budget.json (403 attempts, 19 unknown-usage calls/reservations at this checkpoint). Older production artifacts were deleted at user request; reports/continuation/p4/extraction-removal.json records that historical cleanup. Do not restore deleted production artifacts, delete current diagnostics, reset accounting or mistake engineering success for P4 scientific acceptance. No agent delegation, graph writes, new model downloads or publication is implied.
