Continue this existing project as the lead coding/review collaborator, not as a new project.

Read docs/CONTINUE_HERE.md first, then docs/RESUME.md, AGENTS.md, plan.md and docs/assignments/literature-llm-screening.md. Restore and verify the private Git LFS snapshot before relying on generated files. Preserve my existing .env and LiteLLM credentials. Create/use .venv in the repository root.

I stopped the previous laptop's OpenRouter screening run. I want to continue paper screening and eventually extraction here using my existing LiteLLM GLM 5.3 Flash quota. Inspect the proxy configuration privately, use its exact model alias, and do not call OpenRouter inadvertently. Screening is authorized; finish provider validation and bounded run configuration before resuming. Preserve existing OpenRouter results/provenance and handle the changed cache namespace explicitly.

The latest corpus is litcorpus-68fea35a393fe816 and manuscript candidates are mcandidates-c278c6625ff7c537. Do not restart acquisition, parsing, embedding or old repair assignments. Review the narrow concurrency/budget/cache caveats in CONTINUE_HERE.md, then validate LiteLLM with a bounded call and resume screening all acquired papers and the discovered metadata pool. Report true coverage, selections, unread sources, usage and remaining manual downloads. Existing screening caches are incomplete, not a finished selection.

Our deliverable is one complete manuscript-ready biomedical evidence KG across all four manuscript lineages, not an MVP, AT1-only project or NLP research platform. After screening, finish the existing extraction/verification/evidence-assembly design, offline graph export, separate Neo4j integration and manuscript reports. Full extraction needs its implemented/tested pipeline and explicit run allowance; do not mistake screening authorization for unrestricted inference spending. Keep source uncertainty and unreviewed work visible.

Make ordinary implementation decisions yourself, reuse the existing code, and avoid repeated planning/review loops. Give concise, candid progress updates. First report whether restoration and LiteLLM configuration are ready, which outputs already exist, and the next concrete action; then proceed with the authorized work. Ask only for genuinely missing information. Do not expose credentials, overwrite local changes, or declare unverified work complete.
