# P4 evidence frame / targeted patch repair — 2026-09-23

## Active frame/patch repair — 2026-09-23

The user authorized all required P4 design repairs. `grounded-observations-6` implements pre-extraction source-grounded experiment frames/context recovery, source metadata preservation, bounded observation patches with unchanged-field enforcement, and finding-level disposition separate from bundle coverage. P4 context bounds are eight parts/16,000 serialized characters; frozen P3/source artifacts remain unchanged. Final KG vocabulary and models remain unchanged.

Bounded live validation is running as `trial-4ade5b51625020db` on joint_gene_loss, organoid_endpoints and expression_table, using GLM extraction/Kimi verification with the recorded 180-second deadline. Inspect `data/work/p4-accuracy/frame-patch.log`, the trial status and `frame-patch-state.json`; do not launch duplicates. 94 focused tests and 72 corpus/parser tests passed. The completed intermediate trial `trial-b98e14e6a0495cef` demonstrated a one-field table repair but exposed oversized joint-loss verification and incorrect frame selection. The current run uses two-finding review batches, matching-frame measured-variable enforcement and one bounded frame-amendment pass for missed/context-defective findings. The initial frame diagnostic `trial-9291684d0a824962` was stopped after duplicate field names were detected; fixed named frame slots supersede the list contract, with completed caches and unknown dispatch reservations preserved. No full-corpus restart or accuracy claim yet. Earlier pause text below is historical and superseded by this explicit user authorization.

# P4 targeted corrections and complete context reread — 2026-09-23

**Paused at user request; full reread complete.** The user interrupted targeted correction work to request a complete reread of `chatgpt_langchain_synthesis.md` (1,454 lines), `chatgpt_kg_schema.md` (1,087) and `chatgpt_final_synthesis.md` (3,359). All contents, including appended updates, were read. Hashes and scope conclusions: `reports/continuation/p4/context-review.json`. Reference inputs remain unchanged.

The key scope correction is final synthesis §§41–44/65: no-curator automatic acceptance, precision over recall, a small source-grounded regression set and explicit uncertainty are the operational design. Independently annotated references are needed for numerical accuracy claims; a separate annotation/benchmark project is not a prerequisite to finishing the KG. The subsequent user's bounded assembly/critique/repair and reusable-annotation requests remain in scope. This does not weaken source checks or make model agreement truth.

Before the pause, targeted changes produced `grounded-observations-5` / `spatial-nichelinker-kg-2`: clearer evidence categories and local molecular-form types, source-bound table-cell reference enums, and original mention IDs in repair feedback. The 20-node/29-relation vocabulary is unchanged. `extraction_trial.py` adds explicit failed-call retry and a recorded transport-timeout override that preserves completed call caches; retries run serially. Tests now total **80 passing** (the five focused extraction/annotation test files); Ruff and diff checks passed. No dependency installation, `.env` change, source reprocessing or graph write.

Live state: `trial-240260b02ea099a1` completed three source cases with 15 new calls and three Kimi timeouts. Its organoid field review supported all four observations; direct source inspection found corrected IF protein labels for GFP, Sftpc, RAGE and HOPX, while nuclear Taz remained mistyped. The atlas empty output passed its audit. These are specific diagnostic observations, not global precision/recall. Serial retry `trial-e141d8b129661e28` used a 180-second request deadline; its joint-loss field verification and coverage critic completed, then the process was terminated for the user's pause. Partial successful results remain in the shared call cache; do not mark this retry completed or recreate lost in-flight results.

The retry also exposed a verifier input-contract defect: Kimi confused the intermediate `directness=not_established` enum with evidence directness `FUNCTIONAL`, generating false rejections. The verifier/critic system prompt now includes the input enums and their distinct meanings. Functional intervention evidence does not establish a direct or indirect regulatory mechanism. This latest prompt correction passes engineering checks but has **not** been live-validated. Investigate incorrect panel attribution separately; not every rejection was a vocabulary error.

PID 78077 was terminated and the P4 lock is free. Durable state/log: `data/work/p4-accuracy/targeted-corrections-state.json` and `targeted-corrections.log`. Cumulative attempts at pause: 421; all unknown-token reservations are preserved. Two shared call-cache entries are `IN_FLIGHT_UNKNOWN`; that count includes earlier history, and must not be represented as two new completed calls. Do not automatically retry unresolved in-flight dispatches. Full-corpus extraction remains stopped. The two proposed table/null live regressions were **not run** before the pause. Historical sections below cannot override this state.

---

# Historical P4 automated critique and repair — 2026-09-23

**Implemented and exercised; scientific acceptance remains open.** The latest instruction was “i want this automated..do now.” `grounded-observations-4` now runs source/mention/omission criticism, at most one semantic repair, optional source-grounded local assembly and fresh verification in the ordinary extraction engine. GLM extracts/repairs; YAML selects Kimi for independent reading/verification/criticism. The `.env` file and user provider choices are preserved. No agent delegation, acquisition, parsing, embedding, dependency installation, graph writes or publication occurred.

Read [P4_CRITIQUE_AUTOMATION.md](../P4_CRITIQUE_AUTOMATION.md) and the [live source review](../../reports/continuation/p4/critique-repair-review.md), including exact commands, trial progression, accounting, limitations and artifact paths. The accompanying JSON records hashes and the zero-call replay. Final trial: `data/work/p4-accuracy/trial-2c83876001d2386b/`; log: `data/work/p4-accuracy/critique-repair.log`.

Changed implementation: `src/regkg/extraction/{critique,context_repair,grounded,config,schemas}.py`; `src/regkg/workflows/{extraction,extraction_trial,annotation_candidates}.py`; `configs/{extraction,extraction_schema}.yaml`; `tests/{test_critique_repair,test_extraction}.py`. Documentation updated: AGENTS, plan, continuation/resume/new-agent prompt, automation/annotation/schema/research documents and this handoff. Earlier dirty/untracked work remains preserved. CLAUDE.md remains a relative symlink to AGENTS.md.

Validation: **77 focused synthetic tests passed**, Ruff and `git diff --check` passed. Three live development bundles from three papers completed; both nonempty cases repaired/reverified automatically, but seven joint-loss and four organoid observations remain rejected. The common blocker is primary evidence misclassified as CURATED_LITERATURE, with additional mention/quantity defects. The atlas empty output passed its audit. No end-to-end precision/recall, high-accuracy labels or full-paper recall has been established. Model criticism itself can err.

Final identical replay: **zero new calls**, unchanged cumulative budget. This implementation check used 28 new attempts across four engineering checkpoints; cumulative ledger now records 403 attempts and 19 unknown-usage calls/reservations, with monetary cost unknown. No accounting was reset. Three annotation-candidate records were exported with verified source/result checksums and `training_eligible=false`; the same export files are atomically refreshed rather than accumulating copies. No job remains scheduled and the P4 lock is free.

Remaining P4 scientific work: evidence-classification and gene/protein annotation consistency, quantity semantics, source-preserving overflow handling, independently reviewed complete labels and end-to-end evaluation. Full corpus remains stopped (`restart_authorized: false`); do not mark P4 accepted or advance to P5 based on engineering checks. Historical sections below retain earlier evidence and cannot override this checkpoint.

---

# Historical P4 KG schema alignment and critique research — 2026-09-23

Completed this continuation: fully read the final synthesis and final KG schema, added the shared canonical KG vocabulary/evidence contract, wired it into structured extraction and verification, and repaired the writer that was treating every accepted observation as a regulatory claim. The new `grounded-observations-3` contract keeps source attribution, statement status, observed result, directness and regulatory sign separate. Families/groups/chemicals/phenotypes remain in artifacts; they are not converted to single-gene claims. Legacy records need classification before projection. Relative assessments and study identities remain unresolved instead of fabricated. Details: [KG_SCHEMA_ALIGNMENT.md](../KG_SCHEMA_ALIGNMENT.md).

Changed in this continuation: `configs/kg_schema.yaml`, `configs/extraction_schema.yaml`, `src/regkg/extraction/{kg_contract,grounded,schemas}.py`, `src/regkg/workflows/extraction.py`, `tests/{test_kg_contract,test_grounded_extraction,test_extraction}.py`, plan graph vocabulary/acceptance state, and continuation/research/annotation documentation. The reference documents and existing source/trial artifacts were not edited. No new extraction artifact or live run ID exists for these changes.

Validation commands and results:

```sh
.venv/bin/python -m pytest tests/test_kg_contract.py tests/test_grounded_extraction.py tests/test_extraction.py tests/test_annotation_candidates.py -q
# 63 passed; synthetic/offline tests only
.venv/bin/ruff check src/regkg/extraction/kg_contract.py src/regkg/extraction/grounded.py src/regkg/extraction/schemas.py src/regkg/workflows/extraction.py tests/test_kg_contract.py tests/test_grounded_extraction.py tests/test_extraction.py
# All checks passed
```

The user also asked whether an agent could critique/improve extraction. Primary papers were inspected; the [research update](../P4_ACCURACY_RESEARCH.md#agentic-critique-research-update--2026-09-23) recommends one bounded, source-grounded correction pass plus fresh validation, using existing local assembly only for missing context. This is **recommended, not implemented or scientifically validated**. Current blind source reading/field verification already provide part of this behavior. Neither reviewer agreement nor this assistant's judgment is accuracy ground truth.

P4.7/P4.8 remain open for scientific acceptance of the new contract. Independent complete source labels/scoring, omission/overflow handling, bounded semantic repair, evidence-relative assessments and P5 integration remain pending. Full corpus remains stopped (`restart_authorized: false`); no model calls, provider change, `.env` change, dependency change, source reprocessing or graph write occurred.

---

# P4 accuracy repair and annotation handoff — 2026-09-23

The user authorized both LiteLLM aliases for a bounded trial and then requested an explicit scientific annotation manual plus reusable future encoder labels. Those implementations and bounded trials are complete. **P4 scientific acceptance remains open; full-corpus extraction and encoder training remain stopped. No inference job is left scheduled.**

Read [the source-audit report](../../reports/continuation/p4/accuracy-trial-review.md), its JSON evidence, and [annotation design](../P4_ANNOTATION_DESIGN.md). The report distinguishes corrected defects, remaining model/host limitations, provisional source review and unmeasured accuracy.

Changed capability files: `configs/extraction_schema.yaml`, `configs/extraction_trial.yaml`, `src/regkg/extraction/grounded.py`, `annotations.py`, `schemas.py`, `sources.py`, `validation.py`, `src/regkg/workflows/extraction.py`, `extraction_corpus.py`, `extraction_trial.py`, `annotation_candidates.py`, the optional entity-type filter in `src/regkg/literature/passages.py`, and focused extraction/annotation tests. The shared YAML scientific manual is injected into both models' system messages. Model-facing entity/relation enums and fixed per-observation/per-field verifier slots are generated by Pydantic. PubTator supplies aligned cached entity candidates; this does not execute BioREx as a relation model. Missing species evidence now masks that field without erasing a grounded mention; unknown and conflicting species stay distinct.

Live evidence: `trial-141d947ce20f5c9f` completed six paired development bundles before stopping a known verifier-contract defect; `trial-f6f21b8a9362ff23` completed three paired revised-manual cases (24 calls); `trial-ace4913f024e4184` completed the one-case host-repair replay (four GLM calls, all Kimi stages cached). Each lives under `data/work/p4-accuracy/`, with frozen manifest, results, call provenance and candidate annotation export. The first configured `expression_table` was actually an abstract; no result for that case was completed in the stopped first trial. The revised case uses the actual RASL12 table. These are seven distinct source papers/bundles across repeated trials, not a gold benchmark.

Exact reusable commands (already executed through lock-holding runners; do not duplicate just to obtain a shell spelling):

```sh
SSL_CERT_FILE="$PWD/data/external/tls/project-ca-bundle.pem" .venv/bin/python -m regkg.workflows.extraction_trial --live --case joint_gene_loss --case organoid_endpoints --case expression_table
SSL_CERT_FILE="$PWD/data/external/tls/project-ca-bundle.pem" .venv/bin/python -m regkg.workflows.extraction_trial --live --case joint_gene_loss
.venv/bin/python -m regkg.workflows.annotation_candidates --trial data/work/p4-accuracy/trial-ace4913f024e4184
.venv/bin/python -m pytest tests/test_grounded_extraction.py tests/test_annotation_candidates.py tests/test_extraction.py -q
```

Final focused result: **54 passed**. Earlier combined corpus/extraction/annotation run: 100 passed; the additional species-alias/conflict regression passed afterward. Ruff passed. Tests are synthetic engineering checks; live source outputs remain separate. No dependencies changed, no `.env` changes, no source reparse/acquisition/embedding and no graph writes. The cumulative ledger is preserved at 375 attempts with 18 unknown-token calls/reservations; dollar cost remains unknown.

Candidate exports are not gold and are not training-eligible. Independent complete source annotation/scoring, source-preserving overflow continuation, targeted unresolved-field repair and study-separated training exports remain pending. The project remains incomplete. The following handoff sections are historical and cannot authorize production or override this checkpoint.

---

# P4 implementation and validation — 2026-09-23

## Current instruction — extraction stopped for accuracy repair (2026-09-23)

The user explicitly stopped extraction and requested deletion of generated extraction data and thorough research on entity/relation accuracy, including GLM versus Kimi K3. PID 23074 and its helper were stopped. All `data/{work,processed}/extraction-*` artifacts and P4 response/assembly caches, including previous validation artifacts, were deleted. Six corpus batches had completed; batch seven was interrupted. Source papers, PubTator annotations, P3 screening/parsing/embeddings, `.env`, code and cumulative accounting remain intact. `data/work/p4-corpus/status.json` records cancellation; `reports/continuation/p4/extraction-removal.json` records cleanup.

Read [the accuracy research and repair decision](../P4_ACCURACY_RESEARCH.md). P4 scientific extraction acceptance is reopened. Earlier passing schema/source checks are engineering evidence only; the old extraction artifacts no longer exist. Do not restart extraction, switch providers, or advance accepted evidence to P5 from the historical instructions below. No live GLM/Kimi comparison or accuracy improvement has yet been measured. The active scope is accuracy research and repair design; production remains stopped.

## Historical checkpoint — superseded by the instruction above

## Active execution — full ready-corpus extraction

The user explicitly instructed **start the extraction process**, including PubTator, after P4 validation. Full extraction is now running: **115 papers, 1,133 ready bundles, 57 batches**, using `litellm:prescient.glm-5.3-flash` and cached source-aligned PubTator annotations in extractor and verifier inputs. This supersedes the earlier validation-only/next-P5 sequencing below. Process at launch: PID 23074, with `caffeinate` preventing idle sleep while it runs.

Runner: `.venv/bin/python -m regkg.workflows.extraction_corpus`. Durable status: `data/work/p4-corpus/status.json`; live log: `data/work/p4-corpus/run.log`. The runner holds `data/work/p4.lock`; do not start a duplicate extraction process. It consumes each frozen ready manifest, verifies output artifacts and continues through the full queue, retaining failed/uncertain/rejected outcomes. Completed call caches are reused. No parsing, acquisition or embedding restart. Missing main texts and blocked context cases remain pending outside the ready queue.

The user-authorized corpus allowance is 5,000 cumulative attempts, 120M conservative input tokens and 30M output tokens, enough for 1,133 extractor/verifier operations with bounded retries plus prior calls. The shared durable budget retains all prior usage; model/cache settings and `.env` are unchanged. No provider switch or graph writes. Check the actual process/status/log before reporting progress; the original launch observation confirmed batch 1 running and its first two bundles completed.


## Current reviewed result — 2026-09-23

**P4 implementation and bounded live validation are complete. This is not full-corpus extraction or manuscript-KG completion.** The latest user instruction authorized the targeted repairs and their bounded reruns. The active result is `extraction-e0f05381fe0b5e98`; earlier artifacts below are superseded validation history and must not be treated as accepted KG input.

- Eight bundles from eight papers completed: seven direct-ready bundles and one host-validated assembly child (PMID 41075787). All four manuscript lineages are represented. Every extraction and verifier request completed successfully. Forty-eight unique findings: **28 UNCERTAIN, 20 REJECTED, zero AUTO_ACCEPTED**. No unsupported findings were promoted to fill the graph. Common remaining source limitations are missing experimental context, unresolved species-qualified scoring entities and cited/secondary attribution. These are retained scientific dispositions, not transport/schema failures.
- Strict structured output remains enabled. Pass the authoritative Pydantic-derived JSON Schema to LangChain so the OpenAI-compatible SDK cannot discard a response on a Pydantic root-validator failure; retain raw output and usage, then validate with Pydantic locally. Fixed the inconsistent nonempty-findings/empty-result-status instruction. No permissive parser or alternate model/provider was introduced.
- The model-facing direction field is now `observed_target_change` (internal persisted field `direction` retains that same meaning). The verifier independently reports `source_target_change`; host disagreement rejects the finding. The live verifier now rejects the Sox2-knockout target-sign inversions previously approved. Added conservative rejection of an opposing unambiguous literal source sign, non-regulator subjects for regulatory relations, and status labels masquerading as atomic assertions. Missing table cell references are rejected; units absent from the source remain uncertain; zeros are preserved.
- PubTator3 cached Gene annotations remain in extractor/verifier inputs, with source hashes, raw NCBI IDs and independently checked external/canonical offsets. They do not establish experimental species or regulatory validity.
- The original assembly case produced a valid proposal but exceeded the unchanged 6,000-character source-payload limit. It remains `NEEDS_HUMAN_REVIEW/scientific_payload_too_large`; no source was truncated. Deterministic validation preflight now checks the complete required referenced context before selecting an assembly case. The feasible PMID 41075787 case returned **BUNDLE_VALIDATED**, three model calls, then entered ordinary extraction and verification. A single host-controlled schema correction is permitted inside the existing four-call allowance. Tests and live evidence confirm bounded accounting and raw failure retention. Named repair ledgers preserve the original ledger and shared budget; no budget reset.
- `regkg verify evidence --run extraction-e0f05381fe0b5e98`: VERIFIED. Independent audit checked **57 retained exact source spans**, including rejected/uncertain records. Identical `extract --live --assembly` replay: **REUSED_VERIFIED, zero calls**. The final source-check-only revalidation also made zero calls.
- Live synthetic verifier regression: **PASSED**, one call. Accepted the faithful null finding; rejected reversed subject/target, invented increase and unsupported direct binding. Fixture inputs/results are explicitly engineering-only and never enter real finding artifacts.
- Tests: full suite **259 passed, two Docling deprecation warnings** before the final seven quantity regression cases; final focused suite **79 passed** after all final guards. Ruff passes. Existing real Docling receipt (55 passages with verified offsets) and the 1,133-bundle source audit remain valid; no corpus reparse or embedding restart.
- Cumulative durable P4 validation ledger (including historical failures/repairs): **73 attempts, 319,416 accounted/conservatively reserved input tokens, 138,886 output tokens; dollar price unknown**. Twelve attempts retain unknown-token reservations, not invented zero usage. This turn used 44 attempts including the diagnostic, assembly repairs and one engineering regression. Still within the original 90-attempt/1.5M-input/540k-output allowance. `.env` checksum unchanged, no graph writes, no inference process left running.

### Evidence and replay

Current artifact: `data/processed/extraction-e0f05381fe0b5e98/`.
Logs/receipts under `reports/continuation/p4/`: `final-live.log`, `final-replay.json`, `final-verification.json`, `final-source-audit.json`, `live-verifier-regression.json`, `complete-tests.log`, `complete-focused-tests.log`, and `complete-live.log`.

```bash
SSL_CERT_FILE="$PWD/data/external/tls/project-ca-bundle.pem" \
.venv/bin/python -m regkg extract --run p3selection-89efefdd214ee16e --live --assembly
.venv/bin/python -m regkg verify evidence --run extraction-e0f05381fe0b5e98
```

The next project package is P5 file-first graph integration validation, followed by the required automated corpus expansion and incremental extraction in plan §16. The 115 available full texts/1,133 original ready bundles have **not** all been extracted. The 41 missing selected main texts remain on the P3 download ledger and do not exclude those papers. P4's bounded pipeline acceptance does not waive these remaining manuscript obligations.

## Earlier validation history (superseded)

## Live continuation and PubTator integration — 2026-09-23

- DNS restored. Initial two retry attempts failed TLS validation. Rebuilt the ignored `data/external/tls/project-ca-bundle.pem` from certifi plus all user-supplied `certs/*.crt` roots and issuing certificates; HTTPS now validates. `.env` is unchanged.
- Authoritative model-facing contract: `src/regkg/extraction/schemas.py`, with explicit Pydantic Field descriptions carried directly by LangChain `with_structured_output`. No separate drifting YAML schema or alternate agent framework. Unknown optional context and absent numeric measurements do not prohibit categorical findings; motif/prediction/binding remain separate from regulation.
- Connected existing PubTator3 Gene annotations to both extractor and verifier payloads. Reads checksum-addressed local `pubtator.json` assets, reuses P3 exact/context span-alignment code, retains raw NCBI IDs and external offsets, and independently checks canonical spans. Does not infer experimental species from annotation identity. Missing and unaligned annotations are explicit; original assets remain the record of unmatched annotations. No API refetch or model download. PubTator BioREx relations are discovery aids, not accepted findings.
- Annotation asset hashes and context hash enter the extraction manifest; evidence verification checks pinned annotation assets. Assembly-created bundle annotations are recorded separately in its run manifest.
- First live run `extraction-819db1c10658bc3c`: 12 new calls, seven direct bundles, three NO_RELATION and four INSUFFICIENT_CONTEXT; zero accepted findings. `verify evidence` passes file integrity, not scientific acceptance. Assembly exhausted its four calls after attempting a passage ID where a part ID was required and broad searching. Added explicit part-ID navigation hints; the exhausted case ledger is preserved and is not automatically reset.
- Audited seven cached PubTator assets and 53 aligned gene mentions in those seven bundles; three bundles have no aligned gene mentions. See `reports/continuation/p4/pubtator-audit.json`. Document-level unaligned annotations are counted, never copied as canonical offsets.
- Subsequent direct pass `extraction-a1b13e54883f86b6` completed eight bundles: two FINDINGS, two INSUFFICIENT_CONTEXT, one NO_RELATION and three schema-validation FAILED states (PMIDs 39356034, 42206677, 42261399). Thirteen new calls. Its two AUTO_ACCEPTED rows were scientifically unsuitable (treatment named as a cell_state regulating inferred network metrics). **Do not consume this superseded artifact as accepted KG evidence.** Spot review also found regulator/target reversals missed by the LLM verifier; this is a live quality failure, not acceptance.
- Added a deterministic regulator-entity gate: REGULATES/BINDS subjects must be genes, complexes or families. Revalidated the same cached model responses with **zero new calls** into current artifact `extraction-f75eddccf579efc1`: 12 unique findings, 10 REJECTED, two UNCERTAIN, zero AUTO_ACCEPTED. File/source/span verification passed. Three failed extraction calls remain pending; failure diagnostics now retain safe Pydantic error locations/types without raw inputs or secrets. No graph writes.
- Current state: bounded live validation completed but **P4 is not accepted**. Repair strict-output failures, reversed roles/unsupported directness and verifier quality before any larger run. The assembly case remains exhausted and requires an explicit recorded repair allowance rather than silently resetting its ledger. Keep all failed/uncertain findings and source missingness. Current cumulative budget: 29 attempted calls including four transport failures; monetary usage unknown. No inference process remains running.
- Focused schema/PubTator regression tests: 64 passed. Includes corrupted annotation asset rejection, mismatched external span exclusion and external/canonical offset separation. Historical test/live state below is retained for provenance.


Historical pre-restoration status: **PARTIAL — implementation and offline validation present; live P4 acceptance was blocked by proxy DNS.** At that checkpoint no real extracted findings had been generated. See the live continuation above. Synthetic test findings are engineering fixtures, never manuscript evidence. P4/P5 acceptance is not claimed.

## User scope and next sequence

The user assigned deterministic P4 extraction/verification, the bounded read-only assembly agent, and parser/Docling checks on existing selected sources before corpus expansion. [Plan §16](../../plan.md#16-current-next-assignment-and-required-follow-on-work) records mandatory automated expanded discovery/screening/fetching after P4 and graph-file validation. Missing full texts remain pending and do not exclude papers or block ready-source work.

Parent source selection: `p3selection-89efefdd214ee16e`; parent corpus: `litcorpus-68fea35a393fe816`; manuscript candidates: `mcandidates-c278c6625ff7c537`. All **1,133 ready bundles / 115 papers / 117 canonical documents** passed the P4 audit of manifest outputs, original asset hashes, canonical text, locators, offsets and structured source fields. The audited bundle routes are 1,131 JATS and two Docling PDF bundles. The other 41 selected papers and existing blocked evidence remain pending.

## Implementation

- `src/regkg/extraction/schemas.py`, `config.py`, `runtime.py`: strict extraction/verifier contracts, source-only prompts, selected-provider factory, explicit native structured-output mode, disabled SDK retries, sanitized raw responses, durable reservations and success replay. LiteLLM/OpenRouter reuse `langchain-openai`; Groq is a lazy optional adapter. Provider routing fields remain isolated. Selected LiteLLM alias is unchanged.
- `sources.py`, `validation.py`: source registry and unchanged-source checks, exact quote/offset/cell validation, species-qualified mapping through the existing project snapshot, conservative accepted/uncertain/rejected states, separate claim/evidence identities, explicit unresolved study independence and negative/null outcomes. Nonhuman genes lacking a local mapping remain unresolved; no human-ID substitution.
- `assembly_schemas.py`, `assembly_tools.py`, `assembly.py`: one LangChain `create_agent` with six bounded local tools, explicit `ToolStrategy(handle_errors=False)`, emitted-tool-call accounting (including structured response), model/tool/runtime/history limits, no-progress guard, durable case ledger and host proposal checks. Direct-ready bundles bypass the agent. A validated child enters the same extractor/verifier workflow. Unknown in-flight interruptions require review; they are not automatically reissued or refunded.
- `src/regkg/workflows/extraction.py`, `src/regkg/cli.py`: representative validation or a complete numbered P3 batch; source pinning, capability preflight, extraction, verification, deterministic disposition, Parquet/JSONL persistence, immutable partial-repair lineage, cached replay, and `verify evidence` for hashes/source spans/joins. Empty and failed outcomes remain visible. Successful extraction results are reusable while failed calls require an explicit bounded retry.
- `configs/extraction.yaml`, `pyproject.toml`, `uv.lock`: finite validation settings and pinned agent/Groq optional extras. Installed agent dependency is LangChain 1.4.2 / LangGraph 1.2.12; existing LangChain OpenAI 1.6.3, core 1.6.4 and Docling 2.129.0 retained. Groq extra declared but not installed or live-tested.

The source tools currently navigate the pinned canonical document. Cross-asset/experiment linkage and reviewed semantic blockers remain unresolved unless source-validated; they are never cleared by a model assertion. Table numeric filtering requires host-supplied reviewed units/scale; absent review remains UNSUPPORTED. These limits are explicit, not claims of universal interpretation.

## Actual verification

```text
.venv/bin/python -m pytest -q
  249 passed, 2 Docling deprecation warnings (14.61 s)
.venv/bin/python -m pytest tests/test_extraction.py tests/test_contracts.py -q
  62 passed (1.22 s), after numbered-batch wiring
.venv/bin/python -m ruff check src/regkg/extraction src/regkg/workflows/extraction.py src/regkg/cli.py tests/test_extraction.py
  All checks passed
.venv/bin/python -m pip check
  No broken requirements found
.venv/bin/python -m regkg extract --run p3selection-89efefdd214ee16e --dry-run --assembly
  PREPARED_NO_CALLS; extraction-00315016d570a4b4; seven direct-ready bundles plus one local context investigation
.venv/bin/python -m regkg extract --run p3selection-89efefdd214ee16e --batch 1 --dry-run
  PREPARED_NO_CALLS; extraction-c8b64426a6251e92; all 20 scheduled bundles / 10 papers
```

Tests exercise exact spans, null findings, reversal, speculation, wrong species, unresolved family/complex mapping, fabricated cells, provider routing/strict-schema transport, shared-budget restart/retry, actual SDK agent tool dispatch and structured proposal, parallel-call limit rejection before dispatch, source changes, no-progress/case limits, and a synthetic end-to-end finding → claim → evidence artifact with zero-call replay. Stub transport/agent fixtures do not establish live GLM capability.

A **real PDF supplement** for PMID 33822772 (`jciinsight-6-144652-s279.pdf`, 968,122 bytes) was run through Docling again: **PARSED, 55 passages, all canonical offsets verified**. The original source hash and parser/model metadata are in [the receipt](../../reports/continuation/p4/docling-real.json). No duplicate canonical document/raw export was retained. Existing JATS/BioC/table/ZIP parser regressions passed; the full corpus was not reparsed.

Logs and receipts: [validation directory](../../reports/continuation/p4/), including `tests.log`, `final-focused-tests.log`, `source-audit.json`, `docling-real.json`, `ruff.log`, `dependencies.log`, `network-status.json` and `verification.json`.

## Live state and exact resume

The live capability preflight made two failed transport attempts and **zero successful model calls**. Independent DNS checks fail with `gaierror` / errno 8 for the configured LiteLLM proxy hostname; the request never reached a usable chat endpoint. Existing credentials were preserved. The user was asked asynchronously to restore the required network/VPN. No fallback provider was called.

Reservations are retained in `data/work/p4-validation/budget.json`: unknown usage is not treated as zero or silently refunded. Current shared validation allowance: 90 total attempts, 1,500,000 conservatively reserved input tokens and 540,000 output tokens; at most two extractor/verifier attempts per operation. Assembly has four model/eight tool attempts, 60-second model timeout, 300-second active runtime and 1,000 output-token cap. Its 24,000-byte conservative input bound includes the six schemas and accumulated history (increased from the initial 8,000 planning value to accommodate the actual serialized tool contract). Actual GLM token/cap behavior still needs live validation. LiteLLM dollar price remains unknown under the user's quota.

When proxy DNS/connectivity is restored, resume this **bounded validation only**:

```bash
SSL_CERT_FILE="$PWD/data/external/tls/project-ca-bundle.pem" \
.venv/bin/python -m regkg extract \
  --run p3selection-89efefdd214ee16e --live --assembly --retry-failed
```

Then run `regkg verify evidence --run <actual extraction key>` and repeat the identical extraction command to verify zero-call replay. Review actual live source spans/dispositions and agent outcomes before package acceptance or a larger run. A failed structured-output capability check must be diagnosed; do not switch to permissive JSON parsing or another model silently. P5 export/import and complete-corpus extraction are not implemented by this P4 assignment.

## Preservation

`.env` unchanged; `CLAUDE.md` relative symlink preserved. Prior P3/source/parse/screening/embedding artifacts retained. Only obsolete input-manifest-only dry-run directories from this implementation were removed; the failed preflight, current preparation manifests and useful call/budget records remain. No broad acquisition, new embedding, database writes, commit or push.
