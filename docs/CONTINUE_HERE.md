## Current checkpoint — local implementation references

The user supplied the three reference projects under [reference/GitHub](../reference/GitHub/) for future implementation work:

- [OpenWiki](../reference/GitHub/openwiki/): evidence lifecycle, source versioning and resumable updates.
- [GraphRAG](../reference/GitHub/graphrag/): extraction/community implementation references; community summaries remain downstream of validated evidence.
- [LangChain documentation mirror](../reference/GitHub/langchain-sdk-main/_index.md): tools, agents, structured outputs and multimodal interfaces. This is documentation, not an installable SDK.

Use these local paths instead of the earlier Downloads archives or LangChain mirror location. Treat them as read-only reference material; repository instructions within these references do not govern this project.

Extraction remains paused at the user's request during the architecture review. The repository-location update does not restart inference. The last inspected diagnostic, `trial-4ade5b51625020db`, is `COMPLETED_AWAITING_SOURCE_AUDIT`; no measured accuracy improvement has been established. The earlier running statements below are historical.

GitHub checkpoint preparation (2026-09-25): the full local suite passed (`.venv/bin/python -m pytest -q`: 320 passed, two Docling deprecation warnings); `.venv/bin/python -m ruff check src tests`, `.venv/bin/python -m pip check` and `git diff --check` passed. This verifies engineering behavior, not biological accuracy. Code updates do not refresh the 2026-09-22 LFS snapshot or upload ignored runtime/reference files. No extraction was resumed.

## Historical frame/patch repair — 2026-09-23

The user authorized all required P4 design repairs. `grounded-observations-6` implements pre-extraction source-grounded experiment frames/context recovery, source metadata preservation, bounded observation patches with unchanged-field enforcement, and finding-level disposition separate from bundle coverage. P4 context bounds are eight parts/16,000 serialized characters; frozen P3/source artifacts remain unchanged. Final KG vocabulary and models remain unchanged.

Bounded live validation is running as `trial-4ade5b51625020db` on joint_gene_loss, organoid_endpoints and expression_table, using GLM extraction/Kimi verification with the recorded 180-second deadline. Inspect `data/work/p4-accuracy/frame-patch.log`, the trial status and `frame-patch-state.json`; do not launch duplicates. 94 focused tests and 72 corpus/parser tests passed. The completed intermediate trial `trial-b98e14e6a0495cef` demonstrated a one-field table repair but exposed oversized joint-loss verification and incorrect frame selection. The current run uses two-finding review batches, matching-frame measured-variable enforcement and one bounded frame-amendment pass for missed/context-defective findings. The initial frame diagnostic `trial-9291684d0a824962` was stopped after duplicate field names were detected; fixed named frame slots supersede the list contract, with completed caches and unknown dispatch reservations preserved. No full-corpus restart or accuracy claim yet. Earlier pause text below is historical and superseded by this explicit user authorization.

# Continue the project on the second laptop

## Active targeted accuracy correction — 2026-09-23

**PAUSED BY USER FOR CONTEXT REVIEW.** The latest instruction is to wait and read all three reference synthesis/schema documents completely. The live retry process was stopped; completed calls and cumulative reservations are preserved. `trial-e141d8b129661e28/status.json` and `targeted-corrections-state.json` record the pause. Do not treat the earlier running text below as current execution or automatically retry an `IN_FLIGHT_UNKNOWN` call. Eighty focused tests pass after an additional verifier-input contract clarification; that clarification has not yet been live-validated.

The reread is **complete**: LangChain synthesis 1,454 lines, KG schema 1,087 lines and final synthesis 3,359 lines, including appended clarifications. File hashes and scope conclusions are in `reports/continuation/p4/context-review.json`. Final synthesis §§41–44 and 65 explicitly specify a no-curator, precision-first automatic acceptance workflow with small source regression checks. Independent gold annotation is required to substantiate quantitative accuracy claims, **not a new prerequisite for completing the KG**. Earlier continuation text that makes a separate annotation/evaluation project mandatory overstates the intended scope. Later user-authorized bounded critique/repair and reusable annotation candidates remain in scope; actual encoder training remains separate.

The scientific goal remains source-grounded manuscript evidence and context-aware TF nominations across all four lineages. Preserve the distinction between project predictions, regulatory claims, assay-specific evidence and nomination-relative support/context. Resolve field-contract inconsistencies before treating verifier rejection counts as biological error rates. No more inference was launched after the pause; cumulative attempts at pause were 421, with unknown reservations preserved. No new reference/source files or dependencies were created or modified.

Latest user instruction: **“continue then”**, following the explanation that higher accuracy was not yet demonstrated. `grounded-observations-5` / `spatial-nichelinker-kg-2` clarify evidence-category definitions and local protein/gene typing, bind quantity cell references to supplied source cells, and attach original mention identities to repair feedback. The vocabulary remains 20 labels / 29 relationship types. Seventy-nine focused tests passed before the live check.

The first corrected three-case diagnostic `trial-240260b02ea099a1` completed with three Kimi timeouts; successful GLM/source/verification calls are preserved. The organoid field checks supported all four observations, and a direct source audit confirmed four corrected protein labels; nuclear Taz remained mistyped. Targeted serial retry `trial-e141d8b129661e28` is running with a recorded 180-second transport deadline and unchanged decoding/model choices. Inspect `data/work/p4-accuracy/targeted-corrections-state.json`, the trial status and `targeted-corrections.log` before launching anything. All prior unknown usage remains reserved; full-corpus extraction remains stopped. This is a targeted source regression check, not independent precision/recall validation.

## Historical automation checkpoint — before targeted corrections

Latest user instruction: **“i want this automated..do now.”** The bounded critique/repair workflow is implemented as `grounded-observations-4`; read [P4_CRITIQUE_AUTOMATION.md](P4_CRITIQUE_AUTOMATION.md). It audits empty outputs, every mention and independently read fact, repairs once and reverifies. Source-grounded context requests route automatically to the existing local assembly agent. Ordinary roles resolve to LiteLLM GLM extraction and Kimi verification via YAML; `.env` is preserved.

The live implementation check is complete as `trial-2c83876001d2386b` on three existing development source bundles with the GLM/Kimi role split. Both nonempty cases completed one repair and fresh verification; all eleven final observations remain rejected, primarily for evidence classification, with additional typing/quantity defects. The atlas abstract produced no relation and passed its omission audit. This is not a measured accuracy gain. Read the [source and implementation review](../reports/continuation/p4/critique-repair-review.md), its JSON receipt and [P4 handoff](handoffs/P4.md).

Validation: **77 focused synthetic tests passed**, Ruff and diff checks passed. An identical replay used **zero new calls**, with unchanged cumulative accounting (403 attempts, 19 unknown-usage calls/reservations). Candidate annotations are exported with zero gold/training-eligible labels. No inference job remains scheduled; the P4 lock is free. Log: `data/work/p4-accuracy/critique-repair.log`. Earlier trials `trial-846ed530e6855498`, `trial-f51afb94e0507394` and `trial-77abde8d671abe0b` preserve the diagnosed engineering failures and repairs; do not rerun them or reset the shared budget.

Full-corpus extraction remains stopped. Next scientific work is evidence-classification and gene/protein consistency, quantity semantics and source-preserving overflow handling, followed by independently reviewed complete annotations/precision-recall evaluation. Earlier “repair not implemented” statements below are historical. `.env`, source files and completed P3 processing are preserved; no snapshot restore, acquisition, parsing or embedding is needed.

## Historical schema alignment and critic research — before automation

Read [KG_SCHEMA_ALIGNMENT.md](KG_SCHEMA_ALIGNMENT.md) and the [agentic critique research update](P4_ACCURACY_RESEARCH.md#agentic-critique-research-update--2026-09-23). Both requested final synthesis/schema documents were read completely. `configs/kg_schema.yaml` now provides the final 20-node/29-relationship vocabulary and evidence semantics; `grounded-observations-3` derives evidence contracts from it and adds disease context and an eleventh verifier check. Canonical claim persistence is limited to eligible resolved gene-level findings; other accepted observations remain lossless file artifacts. Claim-relative polarity and nomination-relative context match are not invented globally. P4.7/P4.8 scientific acceptance is reopened for the changed contracts.

Validation: 63 focused synthetic tests passed, Ruff passed. No live inference was performed for these changes and old trial outputs remain immutable. A one-pass source-grounded critique/correction extension is recommended for evaluation but **not implemented or accuracy-validated**. Current code has a blind source reader and field verifier; the only tool-using role remains the existing bounded local assembly agent. No full-corpus restart was authorized; do not mistake research or schema tests for that authorization. `.env`, sources, caches and cumulative usage remain preserved.

## Historical instruction — earlier bounded accuracy trials (2026-09-23)

Latest steering: put scientific annotation expertise into both models' system prompts/schema and preserve candidates for future encoder fine-tuning. Implemented manual `grounded-observations-2`, fixed required verifier slots, and checksum-verified candidate export. Read [P4_ANNOTATION_DESIGN.md](P4_ANNOTATION_DESIGN.md). The original paired run `trial-141d947ce20f5c9f` completed six paired development cases and was stopped when its verifier contract defect was established; completed outputs were preserved. Revised-manual `trial-f6f21b8a9362ff23` completed three paired cases. The final one-case paired replay, `trial-ace4913f024e4184`, completed: four new GLM calls and zero new Kimi calls. It validates the host repair preserving valid mentions while masking unsupported species metadata. No model job remains scheduled. See `reports/continuation/p4/accuracy-trial-review.md`; overall accuracy remains unmeasured. Inspect `data/work/p4-accuracy/manual-trial-status.json` and `manual-trial.log`; do not launch a duplicate. The original case named `expression_table` was an abstract bundle; the revised trial configuration uses the actual source-linked RASL12 table. Neither is a gold benchmark. Existing full-corpus stop remains in force.

The user supplied `prescient.kimi-k3` and `prescient.glm-5.3-flash`, confirmed both are available on LiteLLM, and instructed **try final design**. This authorizes the repaired schema and bounded paired-model validation; the full corpus remains stopped. The ordinary P4 workflow now uses YAML-defined entity/relation types, verified mention IDs, measured-variable observations, independent source reading and field-level verification. See `configs/extraction_schema.yaml` and `src/regkg/extraction/grounded.py`.

Both model aliases passed a real structured-output check. Trial configuration: `configs/extraction_trial.yaml`; runner: `.venv/bin/python -m regkg.workflows.extraction_trial --live` with the existing project TLS bundle. Source trial artifacts/caches are under `data/work/p4-accuracy/`; the trial log is `data/work/p4-accuracy/final-trial.log`. Inspect actual process/status before reporting current execution. Mention recognition uses low effort; observation/verification use high effort. The bounded allowance preserves prior P4 usage and caps cumulative attempts at 500 (6M input/2.5M output tokens; dollar cost unknown). No `.env` change, provider fallback, source reparse or graph write.

The old corpus PID and helper were stopped, and old generated extraction artifacts/caches were deleted at user request. Sources, PubTator assets, P3 preparation and accounting remain intact. The corpus runner now refuses launch while `data/work/p4-corpus/status.json` records `restart_authorized: false`. Passing engineering checks or a diagnostic trial does not authorize production or establish manuscript-level accuracy. Research and proposed broader validation: [P4_ACCURACY_RESEARCH.md](P4_ACCURACY_RESEARCH.md).

## Historical checkpoint — superseded by the instruction above

## Active execution — full ready-corpus extraction

The user explicitly instructed **start the extraction process**, including PubTator, after P4 validation. Full extraction is now running: **115 papers, 1,133 ready bundles, 57 batches**, using `litellm:prescient.glm-5.3-flash` and cached source-aligned PubTator annotations in extractor and verifier inputs. This supersedes the earlier validation-only/next-P5 sequencing below. Process at launch: PID 23074, with `caffeinate` preventing idle sleep while it runs.

Runner: `.venv/bin/python -m regkg.workflows.extraction_corpus`. Durable status: `data/work/p4-corpus/status.json`; live log: `data/work/p4-corpus/run.log`. The runner holds `data/work/p4.lock`; do not start a duplicate extraction process. It consumes each frozen ready manifest, verifies output artifacts and continues through the full queue, retaining failed/uncertain/rejected outcomes. Completed call caches are reused. No parsing, acquisition or embedding restart. Missing main texts and blocked context cases remain pending outside the ready queue.

The user-authorized corpus allowance is 5,000 cumulative attempts, 120M conservative input tokens and 30M output tokens, enough for 1,133 extractor/verifier operations with bounded retries plus prior calls. The shared durable budget retains all prior usage; model/cache settings and `.env` are unchanged. No provider switch or graph writes. Check the actual process/status/log before reporting progress; the original launch observation confirmed batch 1 running and its first two bundles completed.


This is the authoritative continuation checkpoint, recorded 2026-09-22 after the user stopped local screening and requested migration to their existing LiteLLM quota. Later user instructions override historical review stop boundaries. Read this file, AGENTS.md, plan.md, docs/manuscript_scope.md, docs/assignments/literature-llm-screening.md, then the latest handoff/review as needed. Do not replay the historical sequence of repair assignments.

## Active assignment — 2026-09-23

The user now requests **build and test P4 first on the available selected sources**, including deterministic extraction/verification, the bounded read-only assembly agent, parser checks and real Docling validation. Follow [plan §16](../plan.md#16-current-next-assignment-and-required-follow-on-work). This explicitly supersedes historical instructions below to wait for complete expanded P3 before P4 implementation. Pin the ready subset for bounded validation; missing PDFs and blocked evidence stay pending, without blocking ready papers. The selected model remains `litellm:prescient.glm-5.3-flash`; preserve `.env`. No extended crawl has started.

P4 implementation and bounded live validation are now reviewed complete. Read [the current P4 handoff](handoffs/P4.md). Active artifact: `extraction-e0f05381fe0b5e98`, eight completed bundles (seven direct plus one source-validated assembly child), 48 findings: 28 uncertain, 20 rejected, zero accepted. There are no remaining transport/schema execution failures in this validation run. Scientific missingness and rejected findings remain explicit. All four lineages are represented. Source integrity and 57 retained spans were checked; identical replay makes zero new calls. Full tests: 259 passed; final focused checks: 79 passed; live synthetic verifier regression passed. Real Docling validation and the earlier 1,133-bundle source audit remain valid.

LiteLLM GLM is reachable with the existing `.env` and the local CA bundle, which includes the supplied Roche roots and issuing certificates. Pydantic-derived strict JSON Schema, independent target-change verification and cached PubTator gene annotations are active. Do not restart completed P3 preparation or repeatedly rerun validation. The cumulative P4 ledger retains 73 attempts, including failures and repairs; no inference job is left running.

Next: P5 file-first graph integration validation, then mandatory automated discovery expansion, title/abstract screening, eligible fetching/parsing and incremental extraction under plan §16. The full 115-paper available corpus has not been extracted, and 41 selected main texts remain pending. Current 156/115-paper counts are not the comprehensive finish line. No extended crawl or graph import has started.

## Live continuation on this laptop — 2026-09-22

Latest: [P3 selection reconciliation](handoffs/P3-selection-reconciliation.md). The current selection is `p3selection-89efefdd214ee16e`: 156 extraction-selected papers after 11 background/commentary dispositions and two study-version links; 115 have usable main text and 1,133 verified ready bundles; 41 article requests remain. P3 is **not fully accepted/frozen**: missing files and blocked interpretation/partial-source states remain explicit. Read that handoff and its download list before running any more preparation. Reuse the current selection and source caches; do not regenerate superseded drafts. The redundant transfer archive was dehydrated to LFS pointers to save 3.2 GB; restored inputs and local LFS objects remain intact. Normal continuation does not need another `git lfs pull` or snapshot restore.

Restoration is verified and LiteLLM full-pool screening plus targeted failure repair are complete: 2,235 validated records, zero invalid/unread windows, 169 provisional extraction selections. Read [the live continuation handoff](handoffs/P3-litellm-continuation.md) before following the older first-actions checklist below. Use the `litellm_repaired` reports and preserve the original results; do not rerun completed screening. The user confirmed unlimited LiteLLM GLM quota; the prior OpenRouter allowance is superseded. Source-readiness reconciliation, extraction and the overall KG remain unfinished; screened metadata is not full-text evidence.

## Immediate objective and authority

Finish the one complete Spatial NicheLinker biomedical regulatory-evidence KG project. No MVP, no AT1-only finish line, no NLP research expansion. The user authorized LLM paper selection across all acquired papers and discovered metadata. Continue that work on this laptop using its LiteLLM proxy and GLM 5.3 Flash quota. Do not restart acquisition, embedding or source parsing just to reconstruct already transferred work. Do not use local OpenRouter credit by accident.

The previous user wanted implementation and a bounded local test before full extraction on this laptop. Screening is now authorized here; full KG extraction still needs its implemented/tested pipeline, final source selection, explicit run allowance and review. Existing API keys are not blanket authorization for unrelated work. P4 extraction, P5 graph export/import, and P6 manuscript reports remain unfinished. Do not declare the project finished when screening completes.

## Exact state

- P1 and manuscript-wide P2 accepted. Candidate artifact: `mcandidates-c278c6625ff7c537`; ingestion `ingest-6f3b10c688adf3b6`. Original literature baseline `litretrieval-6a092b32d5f87020` accepted.
- Latest preparation: `litcorpus-68fea35a393fe816`, work corpus `corpus-c700d6e584b7bccf`. 452 acquired publications, 2,264 assets, 60/60 identified needed supplements, 1,078 ready bundles from approximately 192 papers, 759 context investigations. These are processing counts, not final evidence or inclusion targets. Many source-specific interpretation/relevance tasks remain open.
- Cache repair and final source fetches were worker-reported complete. They and subsequent screening implementation require proportionate review, not another broad redesign. Historical handoff says no chat calls because it predates screening; actual live screening DID occur afterward.
- `src/regkg/workflows/screening.py` and `src/regkg/literature/screening_llm.py` implement tool-free screening. Three-paper OpenRouter validation report recorded 7 calls, 41,461 input + 10,033 output tokens and reported $0.01123565. An acquired-pool run started and was interrupted at user request. At migration there were 96 cached screening JSON records; this is NOT 96 completed papers. Caches and validation reports are in the snapshot. There may be no final acquired-pool report due to interruption.
- The old screening dry run estimated 814 windows for 452 acquired papers and 1,784 additional discovered metadata records. Recompute only this inexpensive plan if inputs/settings change. Do not impose 192 papers as a ceiling.
- Three current user-download requests: PMIDs 41386231, 42555354, 42327275. Consult the latest actionable_downloads.csv for titles/links/paths. Other listed articles are not automatically requested. The last PMID is the relevant KLF5/basaloid claim with abstract-only content despite attempted JATS routes.
- Habermann supplement is now downloaded; table-family definitions recovered. Source-specific direction interpretations/conflicts remain recorded in configs/p3_table_semantics.yaml. Do not invent missing direction or upgrade expression to regulation.

## First actions

1. Follow docs/RESUME.md: obtain Git LFS objects and restore/verify the snapshot; recreate root .venv from uv.lock. Preserve the destination's existing .env and LiteLLM settings. Do not transfer another environment or overwrite credentials.
2. Inspect the latest code/tests and run appropriate checks. Confirm snapshot manifests and CLI availability. Compare this checkpoint with actual restored files rather than trusting a timestamp or worker status alone.
3. Set `LLM_MODEL=litellm:<exact proxy model alias>` and optionally `SCREENING_MODEL` to the same selector in the destination .env. Set `LITELLM_BASE_URL` and `LITELLM_API_KEY` privately. Discover the exact alias from the user's existing proxy configuration or supported model listing; do not assume the OpenRouter slug is the proxy alias. The other laptop is already configured; inspect presence/configuration without printing keys. No OpenRouter host order, price, reasoning or routing parameters should reach LiteLLM.
4. Review screening runtime safeguards BEFORE a bulk resume. Current Budget.check/spend is shared across ThreadPoolExecutor workers without atomic reservation; a hard spending/call cap is therefore not guaranteed. Add narrow thread-safe reservation/accounting if retaining concurrency. Handle unknown LiteLLM monetary usage explicitly: zero price only if the user's quota/account contract confirms it; otherwise report unknown and enforce call/token ceilings. The old $1/700-call allowance is an OpenRouter run setting, not the intended full-corpus limit.
5. Inspect cache semantics: model/provider is included in prompt_sha, so switching to LiteLLM creates a new cache namespace. Preserve OpenRouter results and provenance. Do NOT rename caches or relabel them as LiteLLM. Reuse completed screening decisions only via explicit provenance-preserving validated import/skip logic with compatible scope/text/schema; if that is not implemented, explain the limited recomputation rather than silently reusing different-model cache entries.
6. Perform one bounded LiteLLM schema/transport smoke test, then resume acquired-paper screening with the configured allowance. Check all retained sections/windows have a completed or explicit unread state. Produce records incrementally and resumably. No new agent framework. Finish discovered-metadata screening as assigned; check whether its execution path is implemented, since a dry-run estimate alone is not execution.
7. Check a small sample of inclusions/exclusions and known controls. Report selected papers, unresolved sources, per-lineage/regulator coverage, calls/tokens/quota usage, and acquisition needs. Only then freeze extraction inputs and implement/validate the remaining extraction pipeline. Do not add repeated manual approval gates to every paper.

## Scientific invariants and finish line

Cover AT1, alveolar macrophages, KRT5-negative/KRT17-positive epithelium, and activated fibrotic fibroblasts. Broad 47-cell-type claims remain context, not invented regulatory evidence. Yale signatures are descriptive pooled niche expression, not donor-aware DE. ChromLinker transform/zeros/aggregation semantics remain unconfirmed; preserve values. Niches are dataset/run-qualified and condition-pooled. Distinguish species, exact cell population, disease, primary/secondary attribution, binding, motifs, expression, predicted versus experimental regulation, and null findings. Source-ID consistency never proves experimental species. Negative controls/expected answers stay OUT of model inputs. Incomplete source bundles cannot be made ready by a model assertion.

P4 uses tool-free extraction and verification plus the already specified bounded read-only evidence-assembly agent. P5 exports Parquet/JSONL and validated CSV graph files before separate local Neo4j loading. P6 produces manuscript claim/evidence and missingness reports. No GNN, web app, general autonomous science agent or NLP benchmark is needed. Keep open obligations visible; no silent deferral of manuscript-required work.

## Collaboration style

Act as the continuing lead collaborator: inspect evidence, make ordinary implementation decisions, fix concrete defects, and move toward execution. Be candid about what is tested versus merely reported. Give concise progress updates. Avoid another broad redesign/review loop. The user is frustrated with time spent preparing; prioritize source-grounded usable results. Check off only what you actually review. You inherit recorded decisions and artifacts, not the prior assistant's private memory; this document and source records are the continuity mechanism.
