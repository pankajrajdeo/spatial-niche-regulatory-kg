# P3 source-gap completion

Read AGENTS.md and `docs/reviews/P3-scope-closure.md`. The identity repair and bounded audit are accepted. Complete the specific source gaps below and correct the closure records in the same pass. This assignment authorizes the listed acquisition/search; do not ask again for permission for these reversible bounded actions.

1. Acquire the verified OA supplement for PMID 41075787 (~32 MB) through the existing acquisition path. Use a narrowly scoped allowance (at most 64 MiB compressed) rather than disabling global size safeguards. Preserve archive traversal protections and bounded expansion (at most 512 MiB total expanded for this exception); report any limit failure. Register source/child checksums and parse states, locate Table S4 and Figure S6 and their experiment context, and update affected dependencies. Do not assume all archive members are relevant or parsed. Reuse caches.
2. Repair the Adams PMID 32832599 table coverage without loading huge tables into an LLM. Stream the already-local delimited assets through their full row range; preserve headers, row locators, exact values and cellType fields. Materialize relevant population/TF rows with their contextual metadata and record complete scan versus selective materialization. Do not silently treat a truncated parse as complete, discard off-filter rows without a recorded policy, or interpret expression rows as regulation. Include a regression with a relevant row after the old limit and bounded memory behavior by construction.
3. Investigate Habermann PMID 32832598 table semantics using local methods/supplement documentation first, then primary-source documentation or the paper's versioned analysis code if necessary. Record sources for gene-column meaning, population/comparator, log base/direction, assay and statistical procedure. Preserve unconfirmed fields as unknown. Do not rerun scientific DE, infer donor-aware analysis, or assign regulation from expression. Include a small source-grounded table example for later P4 validation if its interpretation is established; otherwise keep an explicit interpretation blocker.
4. Run a bounded gap-directed search using the existing clients/cache: at most 10 queries total and 20 new unique paper acquisitions across KRT5-/KRT17+/aberrant-basaloid with TEAD1/KLF5/RFX2, and activated/fibrotic/CTHRC1+ lung fibroblasts with FOSB/TEAD1. Use exact synonyms and the manuscript questions. Save a separate versioned queue, results, overlap and decisions; do not change the accepted P2 queue or launch a general crawl. Prioritize primary regulatory evidence, retain reported context and distinguish background/expression. Lack of a hit is missing literature support, not proof of biological absence. All manuscript cases remain in final reporting even when no direct literature exists.
5. Correct the disposition and download records according to the review: provisional relevance remains provisional; distinguish closed acquisition action from scientific exclusion, table interpretation from parse repair, and linked records from independent work. Reconcile the three new needs with existing stable task IDs. Record remaining required user downloads with links and paths; do not ask for all 176 article files. The OA supplement is an automated supplement acquisition, not an article download.
6. Publish a new immutable corpus artifact only for changed source/parse/selection inputs, preserving prior artifacts and reusing compatible caches. Refresh affected bundles, investigation queues, disposition reports and token estimates. Carry forward audited validation cases by source provenance; separate deterministic blocked-input tests from source-ready validation inputs. Expected answers remain evaluation-only, never model input. Do not claim 18 validation cases or 942 candidate bundles are the final scientific evidence scope.
7. Submit one handoff containing exact changed files, relevant tests, artifact IDs/checksums, before/after task counts, actual user-download requests, resolved/unresolved table semantics, per-lineage evidence coverage and a proposed freeze manifest with explicit outstanding obligations. Do not mark unresolved manuscript-required work completed. Run relevant behavioral tests and the existing suite after implementation changes; do not repeatedly rerun expensive corpus preparation without changed inputs.

Stop for lead review. No P4 implementation or chat calls in this assignment; no graph work, commits, pushes or acceptance checkmarks. No new general triage/ranking framework. This is completion of known source gaps for the one manuscript-ready project, not a separate product version.
