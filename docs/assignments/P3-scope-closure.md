# P3 scope closure and validation selection

Lead assignment, 2026-09-22. Read AGENTS.md and the latest P3 corpus correction review. The per-TF repair in `litcorpus-f8a11dd187098a3d` is accepted. Do not redo it or broaden acquisition. This assignment closes selection/disposition questions before P3 freeze; it does not implement P4 or authorize chat calls.

## Decisions already made

1. One complete manuscript-ready project is required. A validation subset is a checkpoint, not a reduced deliverable. Required work cannot be assigned to an imaginary future version.
2. Identity consistency is not experimental-species verification. Relevant nonhuman/name-only evidence may be extracted with uncertainty retained. It may not become resolved human evidence without source-backed resolution. Truly ambiguous identities, broken parses or insufficient source context remain blocked for positive evidence extraction; they can be explicit abstention tests.
3. A query/lineage association is not the experimental cell type. Broad/off-context mechanisms and secondary accounts must remain distinguishable from direct manuscript evidence. Do not count cancer-cell regulatory experiments as macrophage experiments because macrophages appear in the same caption.

## Execute using existing local sources

1. Inspect the 20 `direct_context_not_in_pool` associations from the current proposal, deduplicating shared source inspections. Record actual local source spans and locators supporting species, cells, attribution and assay, or mark the dimension unknown. Inspect up to six best existing KRT and six activated-fibroblast candidates outside the selected set (prioritize named manuscript TFs and relevant context; do not require human annotation). Record how they were chosen and what remains uninspected. This bounded audit diagnoses selection, not corpus-wide recall or complete scientific adjudication.
2. Replace the current proposal with a reviewable validation selection of at most 10 papers/20 bundles. Prefer manuscript-relevant named regulators and source-interpretable evidence. Include a small number of explicit negative controls for off-context, secondary attribution, unresolved species or motif-versus-regulation distinctions. Permit informative nonhuman/unresolved-name examples without promoting their identity. Do not fill lineage quotas with unrelated papers. Report exact and transferable coverage separately; if KRT remains absent, give concrete reasons and required next actions rather than claiming no relevant literature exists.
3. Include source-grounded expected behavior per selected association: what may be extracted, what must stay unknown, and which interpretations must be rejected. Include selected TF/lineage associations and validation purpose in the manifest, not only in an accompanying prose report. Preserve all co-mentioned per-TF identities. Do not manufacture validated biological findings before extraction/verification. Use existing bundle parts; context outside those parts requires a new validated bundle or an investigation item, not an unsupported expectation.
4. Produce a complete disposition proposal for the current corpus frontier, using existing ledgers and stable IDs. Every pending item must map to: required acquisition/parse repair before its extraction; permitted P4 local-context investigation; extraction/verification with explicitly preserved uncertainty; background/irrelevant/duplicate with reason; or unresolved relevance needing review. Reuse grouping where the same defensible reason applies, but retain individual affected IDs and reconcile totals. Do not automatically classify every unresolved item as unnecessary. Separate prerequisites for any extraction from work required before final manuscript completion. Required unresolved items remain open.
5. Report the actionable manual-download list with titles, links, paths and affected manuscript questions. Distinguish the two current article requests from identification and parse tasks; identify any newly established necessity from the bounded audit. Do not request all advisor papers solely because of membership. No new download/crawl in this assignment; report any concrete acquisition needed for approval.
6. Write `reports/literature/litcorpus-f8a11dd187098a3d/scope_closure.md` and a machine-readable disposition/selection record. Preserve prior proposal files under an explicit snapshot name before replacing them. Record source artifact and input hashes, selection rules, and mapping/ontology hashes if selection uses them. Do not overwrite immutable corpus outputs or run bulk corpus preparation for report-only changes. Update the handoff with exact counts and remaining decisions.

Use a small explicit selection/disposition manifest if that is sufficient; do not build another general ranking system. Modify the existing helper only where needed to implement these decisions. Run relevant tests for changed behavior; docs/report-only changes need integrity and count checks. Stop for lead review. No P4 implementation, paid calls, Neo4j work, acceptance checkmarks, commits or pushes.
